Созданое API уже может получать значения из CoinMarketCap, CoinStats
записывать результат в Базу Данных и после чего читает запись 
из БД выдавая эту же запись по GET запросу.

Необходимо проделать тоже самое, но с оставшимися 3 API.